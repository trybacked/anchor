{
  "name": "@backed/semantic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Burst agentici LLM: profile.json → proposta ontologia",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test:unit": "vitest run --exclude '**/*.integration.test.ts'",
    "test:integration": "vitest run src/propose.integration.test.ts"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.69",
    "@backed/core": "workspace:*",
    "ai": "^7.0.85",
    "zod": "^3.24.1"
  }
}
