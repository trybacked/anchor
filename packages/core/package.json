{
  "name": "@backed/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "modello.yaml schema, tipi condivisi, artefatti run (.backed/)",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test:unit": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
