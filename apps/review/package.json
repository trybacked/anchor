{
  "name": "@backed/review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Review UI: 10 risk-ranked questions, Yes/No/Rename",
  "scripts": {
    "build": "echo 'review app: not configured yet'",
    "typecheck": "echo 'review app: not configured yet'",
    "lint": "eslint src --no-error-on-unmatched-pattern",
    "dev": "echo 'review app: not configured yet'"
  },
  "dependencies": {
    "@backed/core": "workspace:*",
    "@backed/semantic": "workspace:*"
  }
}
